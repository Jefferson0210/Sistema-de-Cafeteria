package com.cafeteria.app.enums;

public enum Rol {
    ADMIN,      // Administrador del sistema
    CAJERO,     // Cajero que emite facturas y registra pagos
    MESERO,     // Mesero que toma pedidos
    CLIENTE     // Cliente que consume
}
